{
  "name": "excel-processor",
  "version": "1.0.0",
  "description": "No-code Excel processing web application",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=8080",
    "dev": "live-server --port=8080 --watch=."
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "xlsx-style": "^0.8.13"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}